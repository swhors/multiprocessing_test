# multiprocessing_test
## 사용법

```angular2html
usage: python main [-h] [-c CMD] [-s SUBCMD] [-a ARGUMENT]

multiprocess_test

optional arguments:
  -h, --help            show this help message and exit
  -c CMD, --cmd CMD     gen/test/del/cnt
  -s SUBCMD, --subcmd SUBCMD
                        test=[simple/data]
  -a ARGUMENT, --argument ARGUMENT
                        data count to generate.
```
